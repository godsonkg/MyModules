#!name=番茄小说直播流拦截（临时测试）
#!desc=2026.09.25-test1 · 拦截请求记录里出现的抖音直播通道，同通道的正常直播也会放不了。测完请关闭。
#!system=ios

# 可以和增强版一起开。要开重写，不需要 MITM，Surge iOS 5.9.1+。
# 用来确认阅读页的直播卡片是不是走这条流，这不是广告专用域名。
# 规则来自请求记录里的 pull-flv-l1.douyincdn.com/stage/stream-*.flv，
# 同时匹配直连域名和“IP/域名/路径”两种明文 HTTP 写法，不封 CDN IP。
# 只覆盖 l1 通道的 .flv，别的通道和格式不管。
# 自检：Safari 打开 http://fanqie-check.invalid/live-test

[URL Rewrite]
^http://(?:(?:\d{1,3}\.){3}\d{1,3}(?::\d+)?/)?pull-flv-l1\.douyincdn\.com(?::\d+)?/stage/stream-\d+\.flv(?:\?|$) _ reject

[Map Local]
^http://fanqie-check\.invalid(?::80)?/live-test(?:\?[^#]*)?$ data-type=text data="直播流临时测试模块 2026.09.25-test1 已加载。同通道的正常直播也会被挡，测完记得关掉。" status-code=200 header="Content-Type:text/plain; charset=utf-8|Cache-Control:no-store"
